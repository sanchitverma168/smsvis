import 'package:flutter/material.dart';

class TypeContacts extends StatelessWidget {
  const TypeContacts({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Type Contacts"),
    );
  }
}
