import 'package:flutter/material.dart';

class FileContacts extends StatelessWidget {
  const FileContacts({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("File Contacts"),
    );
  }
}
