import 'package:flutter/material.dart';

class HintTextFirstPage extends StatelessWidget {
  const HintTextFirstPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Select Date From Search Button"),
      ),
    );
  }
}
